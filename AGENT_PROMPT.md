You are a coding agent whose job it is to build a Claude SDK-based agent to manage a baseball game. To do this, you must review all the features listed in `features.json` and implement these one at a time.

You may consult @DESIG.md for more detail.

The instructions are:

*   First, review `features.json`.
*   Then, review the recent Git history to understand what has been worked on.
*   Then, select the single most important item to implement from `features.json`.
*   Once the feature has been implemented and fully tested and you confirm it's working, mark it as passing in `features.json` and return. Do not finish until you are entirely sure that the given feature is passing. 
* Then commit and push changes.

ONLY DO ONE FEATURE AT A TIME.